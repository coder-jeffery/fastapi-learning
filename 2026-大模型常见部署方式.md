大模型部署主要按**部署环境、服务形态、技术架构**划分，核心是在**效果、成本、延迟、隐私合规**之间做权衡。下面是最常用、工程落地的分类方式：

# 一、按部署环境/位置划分
## 1. 公有云 API 调用（SaaS 化）
- 代表：GPT-4o、Claude、文心一言、通义千问、豆包 API
- 特点：
  - 无需自己训、无需运维，直接调用
  - 按 Token 计费
  - 适合快速上线、中小流量、无强隐私要求
- 适用：ToC 产品、原型验证、轻量应用

## 2. 私有云 / 专有云部署
- 在企业自己的 VPC、专属集群里部署开源模型
- 特点：数据不出域，满足合规，可控性高
- 适用：金融、政务、医疗等强合规行业

## 3. 本地端侧部署（Edge/Device）
- 手机、PC、边缘设备本地跑模型
- 代表：移动端 Qwen、Llama 精简版、Phi-2 等小模型
- 特点：无网络、隐私强、延迟极低
- 限制：只能跑小模型/量化模型

# 二、按服务形态/架构划分
## 1. 独立模型部署（单模型服务）
- 直接用 vLLM/TGI/Text Generation Inference 等框架启一个推理服务
- 适合：单一模型、固定场景

## 2. 模型网关/多模型调度（Model Gateway）
- 上层做路由：根据场景选 API/本地模型/小模型
- 解决：成本控制、灾备、多厂商兼容

## 3. RAG 一体化部署
- 架构：向量库 + 检索服务 + LLM 推理
- 常见组件：Milvus/Chroma/FAISS + FastAPI/Flask + vLLM

## 4. Agent 化部署
- LLM + 工具调用（API/函数/搜索/代码执行）
- 部署要点：工具鉴权、超时、重试、异常回退

# 三、按推理技术方案划分（工程最关键）
## 1. 原生 FP16/BF16 部署
- 效果最好，显存占用高
- 适合小参数量模型或 A100/H100 集群

## 2. 量化部署（主流）
- INT8/INT4/FP8 量化
- 代表：GPTQ、AWQ、SqueezeLLM、bitsandbytes
- 特点：显存大幅下降，速度提升，效果轻微损失

## 3. 推理引擎优化
- vLLM（PagedAttention）、TensorRT-LLM、TGI
- 核心优化：连续批处理、KV cache 复用、动态 batching

## 4. 分布式推理
- 张量并行/流水线并行：单卡放不下时切多卡
- 适用：70B、400B 等超大模型

# 四、按是否微调区分部署
1. **原生基座模型部署**
   - 通用能力，不做业务定制
2. **SFT 微调后部署**
   - 微调权重与基座合并或用 LoRA 加载
3. **LoRA/QLoRA 部署**
   - 只加载小的 LoRA 权重，热切换多任务

# 五、典型落地选型建议
- 快速验证：**公有云 API**
- 企业知识库/合规：**私有云 + 开源模型 + RAG + vLLM**
- 移动端/本地工具：**端侧量化小模型**
- 超大模型：**分布式推理 + 张量并行**

如果你告诉我：**模型大小（7B/13B/70B）、场景（对话/知识库/Agent）、硬件（A10/A100/3090）、合规要求**，我可以给你一套可直接落地的部署架构方案。